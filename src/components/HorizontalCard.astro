---
const { title, img, desc, url, badge, tags, target = '_blank', repoUrl, deployUrl } = Astro.props;
---

<div class="rounded-lg bg-base-100 hover:shadow-xl transition ease-in-out hover:scale-[102%]">
  <a href={url} target={target}>
    <div class="hero-content flex-col md:flex-row">
			{img && <img src={img} alt={title} class="max-w-full md:max-w-[13rem] rounded-lg" />}
      <div class="grow w-full">
        <h1 class="text-xl font-bold">
          {title}
          {badge && <div class="badge badge-secondary mx-2">{badge}</div>}
        </h1>
        <p class="py-1 text-1xl">{desc}</p>
        <div class="card-actions justify-start  mt-2">
					<div class="transition ease-in-out hover:scale-[110%]">
						{repoUrl && <a href={repoUrl} target="_blank" class="btn btn-ghost btn-sm items-center"><img src="/github.svg" alt="Github" class="w-5 ]"></a>}
					</div>
					<div class="transition ease-in-out hover:scale-[110%] mr-2">
						{deployUrl && <a href={deployUrl} target="_blank" class="btn btn-ghost btn-sm mb-2"><img src="/deploy.svg" alt="Github" class="w-5"></a>}
					</div>
					<div class="gap-4 mt-1">
						{tags && tags.map((tag) => <div class="badge badge-outline mr-1.5">{tag}</div>)}
					</div>
				</div>
      </div>
    </div>
  </a>
</div>
